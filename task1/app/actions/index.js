const senderTypingOn = require('./senderTypingOn');
const sendMessage = require('./sendMessage');
const getSender = require('./getSender');
const sendEmail = require('./sendEmail');

module.exports = {
  senderTypingOn,
  sendMessage,
  getSender,
  sendEmail
}
